import { OssIndexServerResult } from '../Types/OssIndexServerResult';
export declare class AuditOSSIndex {
    readonly quiet: boolean;
    readonly json: boolean;
    readonly xml: boolean;
    private formatter;
    constructor(quiet?: boolean, json?: boolean, xml?: boolean);
    auditResults(results: Array<OssIndexServerResult>): boolean;
}
