import { Muncher } from './Muncher';
import { Coordinates } from '../Types/Coordinates';
export declare class NpmList implements Muncher {
    readonly devDependencies: boolean;
    private depsArray;
    constructor(devDependencies?: boolean);
    getDepList(): Promise<any>;
    isValid(): boolean;
    getSbomFromCommand(): Promise<string>;
    getInstalledDeps(): Promise<Array<Coordinates>>;
    private recurseObjectTree;
    private maybePushNewCoordinate;
    private toPurlObjTree;
    private toPurl;
}
